- name: "deploy deployment/{{ application }}-providerkeycloak"
  tags:
    - providerkeycloak
    - deployment
  k8s:
    definition:
      apiVersion: v1
      kind: Deployment
      metadata:
        name: "{{ application }}-providerkeycloak"
        labels:
          component: providerkeycloak
          application: "{{ application }}"
      spec:
        progressDeadlineSeconds: "{{ progress_deadline_seconds }}"
        replicas: "{{ initial_replicas }}"
        revisionHistoryLimit: 0
        strategy:
          type: RollingUpdate
        selector:
          matchLabels:
            component: providerkeycloak
            application: "{{ application }}"
        template:
          metadata:
            labels:
              component: providerkeycloak
              application: "{{ application }}"
              image_tag: "{{ stateless_services_image_tag }}"
              parameters_digest: "{{ (providerkeycloak_configmap_digest + providerkeycloak_secret_digest)|hash('sha1') }}"
          spec:
            serviceAccountName: "run-any-uid"
            ports:
              - name: "keycloak-https"
                containerPort: 8443
                protocol: "TCP"
              - name: "keycloak-http"
                containerPort: 8080
                protocol: "TCP"

            containers:
              - name: "providerkeycloak"
                image: "{{ registry_domain }}/{{ registry_project }}/{{ registry_application }}/identity-provider:{{ stateless_services_image_tag }}"
                imagePullPolicy: "{{ stateless_services_image_pull_policy }}"
                securityContext:
                  readOnlyRootFilesystem: false
                  runAsNonRoot: true
                  runAsUser: 1000
                resources: "{{ providerkeycloak_resources }}"
                envFrom:
                  - configMapRef:
                      name: "{{ application }}-providerkeycloak"
                      optional: true
                  - secretRef:
                      name: "{{ application }}-providerkeycloak"
                      optional: true
                volumeMounts:
                  - name: "providerkeycloak-tmp"
                    mountPath: "/tmp"
            volumes:
              - name: "providerkeycloak-tmp"
                emptyDir:
                  medium: "Memory"

- name: "deploy service/{{ application }}-providerkeycloak"
  tags:
    - providerkeycloak
    - service
  k8s:
    definition:
      apiVersion: v1
      kind: Service
      metadata:
        name: "{{ application }}-providerkeycloak"
        labels:
          application: "{{ application }}"
          component: providerkeycloak
      spec:
        ports:
          - name: "keycloak-https"
            port: 8443
          - name: "keycloak-http"
            port: 8080
        selector:
          application: "{{ application }}"
          component: providerkeycloak
      envFrom:
        - configMapRef:
            name: "{{ application }}-providerkeycloak"
            optional: true
        - secretRef:
            name: "{{ application }}-providerkeycloak"
            optional: true
