- name: "deploy deployment/{{ application }}-identityproviderprovider"
  tags:
    - identityproviderprovider
    - deployment
  k8s:
    definition:
      apiVersion: v1
      kind: Deployment
      metadata:
        name: "{{ application }}-identityproviderprovider"
        labels:
          component: identityproviderprovider
          application: "{{ application }}"
      spec:
        progressDeadlineSeconds: "{{ progress_deadline_seconds }}"
        replicas: "{{ initial_replicas }}"
        revisionHistoryLimit: "{{ initial_replicas }}"
        strategy:
          type: RollingUpdate
        selector:
          matchLabels:
            component: identityproviderprovider
            application: "{{ application }}"
        template:
          metadata:
            labels:
              component: identityproviderprovider
              application: "{{ application }}"
              image_tag: "{{ stateless_services_image_tag }}"
              parameters_digest: "{{ (identityproviderprovider_configmap_digest + identityproviderprovider_secret_digest)|hash('sha1') }}"
          spec:
            serviceAccountName: "run-any-uid"
            ports:
              - name: "keycloak-https"
                containerPort: 8443
                protocol: "TCP"
              - name: "keycloak-http"
                containerPort: 8080
                protocol: "TCP"

            containers:
              - name: "identityproviderprovider"
                image: "{{ registry_domain }}/{{ registry_project }}/{{ registry_application }}/identity-provider:{{ stateless_services_image_tag }}"
                imagePullPolicy: "{{ stateless_services_image_pull_policy }}"
                securityContext:
                  readOnlyRootFilesystem: false
                  runAsNonRoot: true
                  runAsUser: 1000
                resources: "{{ identityproviderprovider_resources }}"
                envFrom:
                  - configMapRef:
                      name: "{{ application }}-identityproviderprovider"
                      optional: true
                  - secretRef:
                      name: "{{ application }}-identityproviderprovider"
                      optional: true
                volumeMounts:
                  - name: "identityproviderprovider-tmp"
                    mountPath: "/tmp"
            volumes:
              - name: "identityproviderprovider-tmp"
                emptyDir:
                  medium: "Memory"

- name: "deploy service/{{ application }}-identityproviderprovider"
  tags:
    - identityproviderprovider
    - service
  k8s:
    definition:
      apiVersion: v1
      kind: Service
      metadata:
        name: "{{ application }}-identityproviderprovider"
        labels:
          application: "{{ application }}"
          component: identityproviderprovider
      spec:
        ports:
          - name: "keycloak-https"
            port: 8443
          - name: "keycloak-http"
            port: 8080
        selector:
          application: "{{ application }}"
          component: identityproviderprovider
      envFrom:
        - configMapRef:
            name: "{{ application }}-identityproviderprovider"
            optional: true
        - secretRef:
            name: "{{ application }}-identityproviderprovider"
            optional: true
