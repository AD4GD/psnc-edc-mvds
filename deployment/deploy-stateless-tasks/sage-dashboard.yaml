- name: Set app.config.json with auth
  set_fact:
    app_config_json: |
      {
        "managementApiUrl": "{{ sage_connector_address }}/management",
        "catalogUrl": "{{ sage_dashboard_address }}/catalog-proxy/catalog/request",
        "theme": "theme-3",
        "deploymentMode": "production",
        "backendUrl": "{{ consumer_backend_address }}/edr-endpoint",
        "oauthIssuer": "{{ provider_keycloak_address }}/realms/Organizations",
        "oauthClientId": "data-space-users"
      }
  when: is_auth

- name: Set app.config.json without auth
  set_fact:
    app_config_json: |
      {
        "managementApiUrl": "{{ sage_connector_address }}/management",
        "catalogUrl": "{{ sage_dashboard_address }}/catalog-proxy/catalog/request",
        "theme": "theme-3",
        "deploymentMode": "production",
        "backendUrl": "{{ consumer_backend_address }}/edr-endpoint"
      }
  when: not is_auth

- name: "deploy configmap/{{ application }}-sagedashboard"
  tags:
    - sagedashboard
    - configmap
  k8s:
    definition:
      apiVersion: v1
      kind: ConfigMap
      metadata:
        name: "{{ application }}-sagedashboard-config"
        labels:
          application: "{{ application }}"
      data:
        app.config.json: "{{ app_config_json | to_nice_json }}"
        nginx.conf: |
          events {}
          pid /tmp/nginx.pid;
          http {
            include /etc/nginx/mime.types;
            server {
              listen 8080;
              server_name {{ sage_dashboard_address_shorten }};
              root /usr/share/nginx/html;
              index index.html;
              access_log off;
              location / {
                try_files $uri $uri/ /index.html;
              }
              location /management {
                proxy_pass {{ sage_connector_address }}/management;
              }
              location /catalog-proxy/ {
                proxy_pass {{ sage_connector_address }}/catalog-proxy/;
              }
              location /catalog {
                proxy_pass {{ sage_connector_address }}/catalog-proxy/catalog/request;
              }
            }
          }

- name: "deploy deployment/{{ application }}-sagedashboard"
  tags:
    - sagedashboard
    - deployment
  k8s:
    definition:
      apiVersion: v1
      kind: Deployment
      metadata:
        name: "{{ application }}-sagedashboard"
        labels:
          component: sagedashboard
          application: "{{ application }}"
      spec:
        progressDeadlineSeconds: "{{ progress_deadline_seconds }}"
        replicas: "{{ initial_replicas }}"
        revisionHistoryLimit: 0
        strategy:
          type: RollingUpdate
        selector:
          matchLabels:
            component: sagedashboard
            application: "{{ application }}"
        template:
          metadata:
            labels:
              component: sagedashboard
              application: "{{ application }}"
              image_tag: "{{ stateless_services_image_tag }}"
              parameters_digest: "{{ (sagedashboard_configmap_digest + sagedashboard_secret_digest)|hash('sha1') }}"
          spec:
            serviceAccountName: "run-any-uid"
            ports:
              - name: "www"
                containerPort: 8080
                protocol: "TCP"
            containers:
              - name: "sagedashboard"
                image: "{{ registry_domain }}/{{ registry_project }}/{{ registry_application }}/data-dashboard:{{ stateless_services_image_tag }}"
                imagePullPolicy: "{{ stateless_services_image_pull_policy }}"
                securityContext:
                  readOnlyRootFilesystem: false
                  runAsNonRoot: true
                # runAsUser: 1000
                resources: "{{ sagedashboard_resources }}"
                envFrom:
                  - secretRef:
                      name: "{{ application }}-sagedashboard"
                      optional: true
                  - configMapRef:
                      name: "{{ application }}-sagedashboard-config"
                      optional: true
                volumeMounts:
                  - name: "sagedashboard-tmp"
                    mountPath: "/tmp"
                  - name: "sagedashboard-nginx-config"
                    mountPath: "/etc/nginx/nginx.conf"
                    subPath: "nginx.conf"
                  - name: "sagedashboard-app-config"
                    mountPath: "/usr/share/nginx/html/assets/config/app.config.json"
                    subPath: "app.config.json"
            volumes:
              - name: "sagedashboard-tmp"
                emptyDir:
                  medium: "Memory"
              - name: "sagedashboard-nginx-config"
                configMap:
                  name: "{{ application }}-sagedashboard-config"
              - name: "sagedashboard-app-config"
                configMap:
                  name: "{{ application }}-sagedashboard-config"

- name: "deploy service/{{ application }}-sagedashboard"
  tags:
    - sagedashboard
    - service
  k8s:
    definition:
      apiVersion: v1
      kind: Service
      metadata:
        name: "{{ application }}-sagedashboard"
        labels:
          application: "{{ application }}"
          component: sagedashboard
      spec:
        ports:
          - name: "www"
            port: 8080
        selector:
          application: "{{ application }}"
          component: sagedashboard
