- name: "deploy deployment/{{ application }}-identityhubprovider"
  tags:
    - connector
    - identityhubprovider
    - deployment
  k8s:
    definition:
      apiVersion: v1
      kind: Deployment
      metadata:
        name: "{{ application }}-identityhubprovider"
        labels:
          component: identityhubprovider
          application: "{{ application }}"
      spec:
        progressDeadlineSeconds: "{{ progress_deadline_seconds }}"
        replicas: "{{ initial_replicas }}"
        strategy:
          type: RollingUpdate
        selector:
          matchLabels:
            component: identityhubprovider
            application: "{{ application }}"
        template:
          metadata:
            labels:
              component: identityhubprovider
              application: "{{ application }}"
              image_tag: "{{ stateless_services_image_tag }}"
              parameters_digest: "{{ (identityhubprovider_configmap_digest + identityhubprovider_secret_digest)|hash('sha1') }}"
          spec:
            serviceAccountName: "run-any-uid"
            ports:
              - name: "api"
                containerPort: 7080
                protocol: "TCP"
              - name: "credentials"
                containerPort: 7081
                protocol: "TCP"
              - name: "identity"
                containerPort: 7082
                protocol: "TCP"
              - name: "did"
                containerPort: 7083
                protocol: "TCP"
              - name: "version"
                containerPort: 7085
                protocol: "TCP"
              - name: "sts"
                containerPort: 7086
                protocol: "TCP"
            containers:
              - name: "identityhubprovider"
                image: "{{ registry_domain }}/{{ registry_project }}/{{ registry_application }}/connector:{{ stateless_services_image_tag }}"
                imagePullPolicy: "{{ stateless_services_image_pull_policy }}"
                securityContext:
                  readOnlyRootFilesystem: false
                  runAsNonRoot: true
                  runAsUser: 1000
                resources: "{{ identityhubprovider_resources }}"
                envFrom:
                  - configMapRef:
                      name: "{{ application }}-identityhubprovider"
                      optional: true
                  - secretRef:
                      name: "{{ application }}-identityhubprovider"
                      optional: true
                volumeMounts:
                  - name: "identityhubprovider-tmp"
                    mountPath: "/tmp"
            volumes:
              - name: "identityhubprovider-tmp"
                emptyDir:
                  medium: "Memory"

- name: "deploy service/{{ application }}-identityhubprovider"
  tags:
    - connector
    - identityhubprovider
    - service
  k8s:
    definition:
      apiVersion: v1
      kind: Service
      metadata:
        name: "{{ application }}-identityhubprovider"
        labels:
          application: "{{ application }}"
          component: identityhubprovider
      spec:
        ports:
          - name: "api"
            port: 7080
          - name: "credentials"
            port: 7081
          - name: "identity"
            port: 7082
          - name: "did"
            port: 7083
          - name: "version"
            port: 7085
          - name: "sts"
            port: 7085
        selector:
          application: "{{ application }}"
          component: identityhubprovider
