- name: "deploy deployment/{{ application }}-consumerkeycloak"
  tags:
    - consumerkeycloak
    - deployment
  k8s:
    definition:
      apiVersion: v1
      kind: Deployment
      metadata:
        name: "{{ application }}-consumerkeycloak"
        labels:
          component: consumerkeycloak
          application: "{{ application }}"
      spec:
        progressDeadlineSeconds: "{{ progress_deadline_seconds }}"
        replicas: "{{ initial_replicas }}"
        revisionHistoryLimit: 0
        strategy:
          type: RollingUpdate
        selector:
          matchLabels:
            component: consumerkeycloak
            application: "{{ application }}"
        template:
          metadata:
            labels:
              component: consumerkeycloak
              application: "{{ application }}"
              image_tag: "{{ stateless_services_image_tag }}"
              parameters_digest: "{{ (consumerkeycloak_configmap_digest + consumerkeycloak_secret_digest)|hash('sha1') }}"
          spec:
            serviceAccountName: "run-any-uid"
            ports:
              - name: "keycloak-https"
                containerPort: 8443
                protocol: "TCP"
              - name: "keycloak-http"
                containerPort: 8080
                protocol: "TCP"

            containers:
              - name: "consumerkeycloak"
                image: "{{ registry_domain }}/{{ registry_project }}/{{ registry_application }}/identity-provider:{{ stateless_services_image_tag }}"
                imagePullPolicy: "{{ stateless_services_image_pull_policy }}"
                securityContext:
                  readOnlyRootFilesystem: false
                  runAsNonRoot: true
                  runAsUser: 1000
                resources: "{{ consumerkeycloak_resources }}"
                envFrom:
                  - configMapRef:
                      name: "{{ application }}-consumerkeycloak"
                      optional: true
                  - secretRef:
                      name: "{{ application }}-consumerkeycloak"
                      optional: true
                volumeMounts:
                  - name: "consumerkeycloak-tmp"
                    mountPath: "/tmp"
            volumes:
              - name: "consumerkeycloak-tmp"
                emptyDir:
                  medium: "Memory"

- name: "deploy service/{{ application }}-consumerkeycloak"
  tags:
    - consumerkeycloak
    - service
  k8s:
    definition:
      apiVersion: v1
      kind: Service
      metadata:
        name: "{{ application }}-consumerkeycloak"
        labels:
          application: "{{ application }}"
          component: consumerkeycloak
      spec:
        ports:
          - name: "keycloak-https"
            port: 8443
          - name: "keycloak-http"
            port: 8080
        selector:
          application: "{{ application }}"
          component: consumerkeycloak
      envFrom:
        - configMapRef:
            name: "{{ application }}-consumerkeycloak"
            optional: true
        - secretRef:
            name: "{{ application }}-consumerkeycloak"
            optional: true
