- name: "deploy deployment/{{ application }}-dataspacekeycloak"
  tags:
    - dataspacekeycloak
    - deployment
  k8s:
    definition:
      apiVersion: v1
      kind: Deployment
      metadata:
        name: "{{ application }}-dataspacekeycloak"
        labels:
          component: dataspacekeycloak
          application: "{{ application }}"
      spec:
        progressDeadlineSeconds: "{{ progress_deadline_seconds }}"
        replicas: "{{ initial_replicas }}"
        revisionHistoryLimit: 0
        strategy:
          type: RollingUpdate
        selector:
          matchLabels:
            component: dataspacekeycloak
            application: "{{ application }}"
        template:
          metadata:
            labels:
              component: dataspacekeycloak
              application: "{{ application }}"
              image_tag: "{{ stateless_services_image_tag }}"
              parameters_digest: "{{ (dataspacekeycloak_configmap_digest + dataspacekeycloak_secret_digest)|hash('sha1') }}"
          spec:
            serviceAccountName: "run-any-uid"
            ports:
              - name: "keycloak-https"
                containerPort: 8443
                protocol: "TCP"
              - name: "keycloak-http"
                containerPort: 8080
                protocol: "TCP"

            containers:
              - name: "dataspacekeycloak"
                image: "{{ registry_domain }}/{{ registry_project }}/{{ registry_application }}/identity-provider:{{ stateless_services_image_tag }}"
                imagePullPolicy: "{{ stateless_services_image_pull_policy }}"
                securityContext:
                  readOnlyRootFilesystem: false
                  runAsNonRoot: true
                  runAsUser: 1000
                resources: "{{ dataspacekeycloak_resources }}"
                envFrom:
                  - configMapRef:
                      name: "{{ application }}-dataspacekeycloak"
                      optional: true
                  - secretRef:
                      name: "{{ application }}-dataspacekeycloak"
                      optional: true
                volumeMounts:
                  - name: "dataspacekeycloak-tmp"
                    mountPath: "/tmp"
            volumes:
              - name: "dataspacekeycloak-tmp"
                emptyDir:
                  medium: "Memory"

- name: "deploy service/{{ application }}-dataspacekeycloak"
  tags:
    - dataspacekeycloak
    - service
  k8s:
    definition:
      apiVersion: v1
      kind: Service
      metadata:
        name: "{{ application }}-dataspacekeycloak"
        labels:
          application: "{{ application }}"
          component: dataspacekeycloak
      spec:
        ports:
          - name: "keycloak-https"
            port: 8443
          - name: "keycloak-http"
            port: 8080
        selector:
          application: "{{ application }}"
          component: dataspacekeycloak
      envFrom:
        - configMapRef:
            name: "{{ application }}-dataspacekeycloak"
            optional: true
        - secretRef:
            name: "{{ application }}-dataspacekeycloak"
            optional: true
