# Copy to .env.secrets
# Used only for local Identity Hub Vault profile.

# Generated by scripts/vault-init-unseal.sh on first run
VAULT_UNSEAL_KEY=
VAULT_ROOT_TOKEN=

# Single service token injected into both Connector and Identity Hub configs
VAULT_SERVICE_TOKEN=
