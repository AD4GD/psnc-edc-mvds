apiVersion: v1
kind: Service
metadata:
  name: "{{ application }}-dashboard"
  labels:
    app: "{{ application }}"
    component: dashboard
spec:
  selector:
    app: "{{ application }}"
    component: dashboard
  ports:
    - name: www
      port: 8080
      targetPort: www
