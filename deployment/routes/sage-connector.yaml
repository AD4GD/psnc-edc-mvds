- name: Create route for sage-connector-api
  k8s:
    state: present
    definition:
      apiVersion: route.openshift.io/v1
      kind: Route
      metadata:
        name: sage-connector-api
        namespace: "{{ k8s_namespace }}"
        annotations:
          haproxy.router.openshift.io/ip_whitelist: ''
      spec:
        host: "{{ sage_connector_address | regex_replace('^https?://', '') }}"
        path: /api
        to:
          kind: Service
          name: "{{ application }}-connectorsage"
        port:
          targetPort: api
        tls:
          termination: edge
          insecureEdgeTerminationPolicy: Allow

- name: Create route for sage-connector-catalog-proxy
  k8s:
    state: present
    definition:
      apiVersion: route.openshift.io/v1
      kind: Route
      metadata:
        annotations:
          haproxy.router.openshift.io/ip_whitelist: ''
        name: sage-connector-catalog-proxy
        namespace: "{{ k8s_namespace }}"
      spec:
        host: "{{ sage_connector_address | regex_replace('^https?://', '') }}"
        path: /catalog-proxy
        to:
          kind: Service
          name: "{{ application }}-connectorsage"
        port:
          targetPort: catalogproxy
        tls:
          termination: edge
          insecureEdgeTerminationPolicy: Allow

- name: Create route for sage-connector-control
  k8s:
    state: present
    definition:
      apiVersion: route.openshift.io/v1
      kind: Route
      metadata:
        name: sage-connector-control
        namespace: "{{ k8s_namespace }}"
        annotations:
          haproxy.router.openshift.io/ip_whitelist: ''
      spec:
        host: "{{ sage_connector_address | regex_replace('^https?://', '') }}"
        path: /control
        to:
          kind: Service
          name: "{{ application }}-connectorsage"
        port:
          targetPort: control
        tls:
          termination: edge
          insecureEdgeTerminationPolicy: Allow

- name: Create route for sage-connector-management
  k8s:
    state: present
    definition:
      apiVersion: route.openshift.io/v1
      kind: Route
      metadata:
        name: sage-connector-management
        namespace: "{{ k8s_namespace }}"
        annotations:
          haproxy.router.openshift.io/ip_whitelist: ''
      spec:
        host: "{{ sage_connector_address | regex_replace('^https?://', '') }}"
        path: /management
        to:
          kind: Service
          name: "{{ application }}-connectorsage"
        port:
          targetPort: management
        tls:
          termination: edge
          insecureEdgeTerminationPolicy: Allow

- name: Create route for sage-connector-protocol
  k8s:
    state: present
    definition:
      apiVersion: route.openshift.io/v1
      kind: Route
      metadata:
        annotations:
          haproxy.router.openshift.io/ip_whitelist: ''
        name: sage-connector-protocol
        namespace: "{{ k8s_namespace }}"
      spec:
        host: "{{ sage_connector_address | regex_replace('^https?://', '') }}"
        path: /protocol
        to:
          kind: Service
          name: "{{ application }}-connectorsage"
        port:
          targetPort: protocol
        tls:
          termination: edge
          insecureEdgeTerminationPolicy: Allow

- name: Create route for sage-connector-public
  k8s:
    state: present
    definition:
      apiVersion: route.openshift.io/v1
      kind: Route
      metadata:
        annotations:
          haproxy.router.openshift.io/ip_whitelist: ''
        name: sage-connector-public
        namespace: "{{ k8s_namespace }}"
      spec:
        host: "{{ sage_connector_address | regex_replace('^https?://', '') }}"
        path: /public
        to:
          kind: Service
          name: "{{ application }}-connectorsage"
        port:
          targetPort: public
        tls:
          termination: edge
          insecureEdgeTerminationPolicy: Allow
