- name: Create route for dataspacehub
  k8s:
    state: present
    definition:
      apiVersion: route.openshift.io/v1
      kind: Route
      metadata:
        name: dataspacehub
        namespace: "{{ k8s_namespace }}"
      spec:
        host: "{{ data_space_hub_address | regex_replace('^https?://', '') }}"
        to:
          kind: Service
          name: "{{ application }}-dataspacehub"
        port:
          targetPort: receiver
        tls:
          termination: edge
          insecureEdgeTerminationPolicy: Allow
