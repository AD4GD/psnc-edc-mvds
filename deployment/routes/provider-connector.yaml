- name: Create route for provider-connector-api
  k8s:
    state: present
    definition:
      apiVersion: route.openshift.io/v1
      kind: Route
      metadata:
        name: provider-connector-api
        namespace: "{{ k8s_namespace }}"
        annotations:
          haproxy.router.openshift.io/ip_whitelist: ''
      spec:
        host: "{{ provider_connector_address | regex_replace('^https?://', '') }}"
        path: /api
        to:
          kind: Service
          name: "{{ application }}-connectorprovider"
        port:
          targetPort: api
        tls:
          termination: edge
          insecureEdgeTerminationPolicy: Allow

- name: Create route for provider-connector-control
  k8s:
    state: present
    definition:
      apiVersion: route.openshift.io/v1
      kind: Route
      metadata:
        name: provider-connector-control
        namespace: "{{ k8s_namespace }}"
        annotations:
          haproxy.router.openshift.io/ip_whitelist: ''
      spec:
        host: "{{ provider_connector_address | regex_replace('^https?://', '') }}"
        path: /control
        to:
          kind: Service
          name: "{{ application }}-connectorprovider"
        port:
          targetPort: control
        tls:
          termination: edge
          insecureEdgeTerminationPolicy: Allow

- name: Create route for provider-connector-management
  k8s:
    state: present
    definition:
      apiVersion: route.openshift.io/v1
      kind: Route
      metadata:
        name: provider-connector-management
        namespace: "{{ k8s_namespace }}"
        annotations:
          haproxy.router.openshift.io/ip_whitelist: ''
      spec:
        host: "{{ provider_connector_address | regex_replace('^https?://', '') }}"
        path: /management
        to:
          kind: Service
          name: "{{ application }}-connectorprovider"
        port:
          targetPort: management
        tls:
          termination: edge
          insecureEdgeTerminationPolicy: Allow

- name: Create route for provider-connector-protocol
  k8s:
    state: present
    definition:
      apiVersion: route.openshift.io/v1
      kind: Route
      metadata:
        annotations:
          haproxy.router.openshift.io/ip_whitelist: ''
        name: provider-connector-protocol
        namespace: "{{ k8s_namespace }}"
      spec:
        host: "{{ provider_connector_address | regex_replace('^https?://', '') }}"
        path: /protocol
        to:
          kind: Service
          name: "{{ application }}-connectorprovider"
        port:
          targetPort: protocol
        tls:
          termination: edge
          insecureEdgeTerminationPolicy: Allow

- name: Create route for provider-connector-public
  k8s:
    state: present
    definition:
      apiVersion: route.openshift.io/v1
      kind: Route
      metadata:
        annotations:
          haproxy.router.openshift.io/ip_whitelist: ''
        name: provider-connector-public
        namespace: "{{ k8s_namespace }}"
      spec:
        host: "{{ provider_connector_address | regex_replace('^https?://', '') }}"
        path: /public
        to:
          kind: Service
          name: "{{ application }}-connectorprovider"
        port:
          targetPort: public
        tls:
          termination: edge
          insecureEdgeTerminationPolicy: Allow
