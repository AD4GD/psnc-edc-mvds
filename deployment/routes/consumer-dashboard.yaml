- name: Create route for consumer-dashboard
  k8s:
    state: present
    definition:
      apiVersion: route.openshift.io/v1
      kind: Route
      metadata:
        name: consumer-dashboard
        namespace: "{{ k8s_namespace }}"
        annotations:
          haproxy.router.openshift.io/ip_whitelist: ''
      spec:
        host: "{{ consumer_dashboard_address | regex_replace('^https?://', '') }}"
        to:
          kind: Service
          name: "{{ application }}-consumerdashboard"
        port:
          targetPort: www
        tls:
          termination: edge
          insecureEdgeTerminationPolicy: Allow

- name: Create route for consumer-dashboard-catalog-proxy
  k8s:
    state: present
    definition:
      apiVersion: route.openshift.io/v1
      kind: Route
      metadata:
        name: consumer-dashboard-catalog-proxy
        namespace: "{{ k8s_namespace }}"
        annotations:
          haproxy.router.openshift.io/ip_whitelist: ''
      spec:
        host: "{{ consumer_dashboard_address | regex_replace('^https?://', '') }}"
        path: /catalog-proxy
        to:
          kind: Service
          name: "{{ application }}-consumerdashboard"
        port:
          targetPort: www
        tls:
          termination: edge
          insecureEdgeTerminationPolicy: Allow

- name: Create route for consumer-dashboard-catalog
  k8s:
    state: present
    definition:
      apiVersion: route.openshift.io/v1
      kind: Route
      metadata:
        name: consumer-dashboard-catalog
        namespace: "{{ k8s_namespace }}"
        annotations:
          haproxy.router.openshift.io/ip_whitelist: ''
      spec:
        host: "{{ consumer_dashboard_address | regex_replace('^https?://', '') }}"
        path: /catalog
        to:
          kind: Service
          name: "{{ application }}-consumerdashboard"
        port:
          targetPort: www
        tls:
          termination: edge
          insecureEdgeTerminationPolicy: Allow
