- name: Create route for consumer-keycloak
  k8s:
    state: present
    definition:
      apiVersion: route.openshift.io/v1
      kind: Route
      metadata:
        name: consumer-keycloak-route
        namespace: "{{ k8s_namespace }}"
        annotations:
          haproxy.router.openshift.io/ip_whitelist: ''
      spec:
        host: "{{ consumer_keycloak_address | regex_replace('^https?://', '') }}"
        to:
          kind: Service
          name: "{{ application }}-consumerkeycloak"
          weight: 100
        port:
          targetPort: keycloak-http
        tls:
          termination: edge
          insecureEdgeTerminationPolicy: Allow
        wildcardPolicy: None
