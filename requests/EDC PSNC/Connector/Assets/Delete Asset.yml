info:
  name: Delete Asset
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{PROVIDER_CONNECTOR_MANAGEMENT_URL}}/v3/assets/{{asset_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
