info:
  name: Get Asset
  type: http
  seq: 2

http:
  method: GET
  url: "{{PROVIDER_CONNECTOR_MANAGEMENT_URL}}/v3/assets/{{asset-id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
