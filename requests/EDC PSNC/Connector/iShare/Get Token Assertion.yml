info:
  name: Get Token Assertion
  type: http
  seq: 2

http:
  method: GET
  url: "{{CONNECTOR_API}}/ishare/token/assertion"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
