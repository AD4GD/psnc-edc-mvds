info:
  name: Delete FC participant
  type: http
  seq: 2

http:
  method: DELETE
  url: "{{FEDERATED_CATALOG_TARGETS_URL}}/api/targets/v1/targets/{{CONSUMER_IH_BASE64}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
