info:
  name: Add FC participant (consumer)
  type: http
  seq: 3

http:
  method: POST
  url: "{{FEDERATED_CATALOG_TARGETS_URL}}/api/targets/v1/targets"
  body:
    type: json
    data: |-
      {
          "participantId": "{{CONSUMER_DID}}",
          "url": "{{CONSUMER_DSP_URL}}",
          "supportedProtocols": ["dataspace-protocol-http"]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
