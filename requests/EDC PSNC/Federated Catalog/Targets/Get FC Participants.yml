info:
  name: Get FC Participants
  type: http
  seq: 1

http:
  method: GET
  url: "{{FEDERATED_CATALOG_TARGETS_URL}}/api/targets/v1/targets"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
