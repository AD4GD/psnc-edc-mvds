info:
  name: Get Transfer Data Credentials
  type: http
  seq: 7

http:
  method: GET
  url: "{{CONSUMER_URL}}/management/v3/edrs/{{pull-transfer-id}}/dataaddress"
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
