info:
  name: Get Registration Request
  type: http
  seq: 3

http:
  method: GET
  url: "{{DATA_SPACE_HUB_URL}}/api/v1/registration/request/list"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
