info:
  name: Confirm Participant Registration Request
  type: http
  seq: 4

http:
  method: PUT
  url: "{{DATA_SPACE_HUB_URL}}/api/v1/registration/request/{{participant-registration-request-id}}/approve"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
