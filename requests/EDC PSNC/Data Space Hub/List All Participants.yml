info:
  name: List All Participants
  type: http
  seq: 2

http:
  method: GET
  url: "{{DATA_SPACE_HUB_URL}}/api/v1/participants/list?offset=0"
  params:
    - name: offset
      value: "0"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
